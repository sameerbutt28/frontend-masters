import Pet from "./Pet"
const Results = ({pets}) => { //here the pet in jsx tells us that we are expecting a props named as pets on the other component where we want to call it.
  return (
    <div className="search">
      
    </div>
  )
}

export default Results
